import type { FC } from 'react'

import type { I{{pascalCase name}} } from './{{pascalCase name}}.types'
import * as S from './{{pascalCase name}}.styles'

export const {{pascalCase name}}: FC<I{{pascalCase name}}> = ({ foo = 'bar' }) => {
  return <S.Title>{foo}</S.Title>
}
